#include <iostream>

using namespace std;

class Solution {
public:
    string convert(string s, int numRows) {
        if (numRows >= s.length() || numRows <= 1) return s;
        string result;
        for (int i = 0; i < numRows; ++i) {

        }
        return ;
    }
};
