#include <iostream>

using namespace std;

class SOlution {
public:
    int maximalSquare(vector<vector<char>>& matrix) {
        int m = matrix.size();
        int n = matrix.size();
        return ;
    }
};
