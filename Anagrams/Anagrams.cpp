#include <iostream>

using namespace std;

class Solution {
public:
    vector<string> anagrams(vector<string>& strs) {
        if (strs.size() <= 1) return strs;
        vector<string> result(0);

        return result;
    }
};
